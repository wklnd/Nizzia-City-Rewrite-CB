const strength = require('strength');
const defense = require('defense');
const dexterity = require('dexterity');
const speed = require('speed'); 



// Target stats:
const targetStrength = 0; 
const targetDefense = 0;
const targetDexterity = 0;
const targetSpeed = 0;

const targetStatus = {
    drunk : false,
    sleepy: false,
    unfocused: false,
    old : false,
    sick : false,
}

const targetAggression = 0 // 0 - 1.0

